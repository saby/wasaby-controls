import { IAbstractListState } from 'Controls-DataEnv/abstractList';

const actions: IAbstractListState['itemActions'] = [
    {
        actionName: 'Controls/actions:Remove',
    },
];

export default actions;
