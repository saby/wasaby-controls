import Base from './../Base/Index';

export default class Index extends Base {
    protected _afterLength: number = 20;
    protected _beforeLength: number = 5;
    protected _marginValue: string = '50px';
}
