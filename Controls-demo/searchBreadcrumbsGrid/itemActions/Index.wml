<Controls.searchBreadcrumbsGrid:View
        storeId="listData"
        backgroundStyle="unaccented"
        columns="{{ _columns }}"
        header="{{ _header }}"
        itemActions="{{_itemActions}}"
/>
