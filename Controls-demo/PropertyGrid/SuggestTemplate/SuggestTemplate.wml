<Controls.dropdown:Combobox
        displayProperty="title"
        keyProperty="title"
        source="{{_source}}"
        bind:selectedKey="selectedKey"
        on:selectedKeyChanged="selectedKeyChanged()"
        attr:class="demo-TextBox js-Controls-PropertyGridEditor__options-{{_options.caption}}"/>