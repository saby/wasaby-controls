<div>
   <Controls.input:Label caption="{{_options.caption}}" attr:class="demo-TextBox"/>
   <Controls.input:TimeInterval value="{{_options.value}}"
                                mask="HH:MM"
                                readOnly="{{_options.readOnly}}"
                                on:valueChanged="_valueChangedHandler()"/>
</div>
