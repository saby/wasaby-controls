<Controls.tree:View
        source="{{_viewSource}}"
        childrenProperty="children"
        parentProperty="parent"
        nodeProperty="type"
        navigation="{{_navigation}}"
        scope="{{_options}}"/>
