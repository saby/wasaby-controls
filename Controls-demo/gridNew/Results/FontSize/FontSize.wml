<div class="controlsDemo__wrapper">
    <Controls.grid:View
            keyProperty="key"
            source="{{_viewSource}}"
            header="{{_header}}"
            columns="{{_columns}}"
            dataLoadCallback="{{ _dataLoadCallback }}"
            resultsPosition="top">
    </Controls.grid:View>
</div>
