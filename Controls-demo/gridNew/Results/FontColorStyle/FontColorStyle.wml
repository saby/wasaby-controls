<div class="controlsDemo__wrapper">
    <Controls.grid:View
            keyProperty="key"
            header="{{_header}}"
            source="{{_viewSource}}"
            columns="{{_columns}}"
            dataLoadCallback="{{ _dataLoadCallback }}"
            resultsPosition="top">
    </Controls.grid:View>
</div>
