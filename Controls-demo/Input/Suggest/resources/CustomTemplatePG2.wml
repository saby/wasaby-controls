<div class="controls-demo__SuggestPG-item">
    <ws:if data="{{itemData.getPropValue(item.contents, 'city')}}">
        <div><b>{{itemData.getPropValue(item.contents, 'city')}}</b></div>
        <div><i>The best city in the World</i></div>
    </ws:if>
    <ws:if data="{{itemData.getPropValue(item.contents, 'name')}}">
        <div><b>{{itemData.getPropValue(item.contents, 'name')}}</b></div>
        <div><i>The best name in the World</i></div>
    </ws:if>
</div>