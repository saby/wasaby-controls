<Controls.explorer:View itemActions="{{_itemActions}}"
                   header="{{_header}}"
                   source="{{_source}}"
                   columns="{{_columns}}"
                   nodeProperty="parent@"
                   parentProperty="parent"
                   keyProperty="id" />
