<Controls.grid:View
   displayProperty="title"
   keyProperty="id"
   source="{{_source}}"
   columns="{{_columns}}"
   actionAlignment="vertical"
   actionCaptionPosition="right"
   itemActions="{{_itemActions}}" />
