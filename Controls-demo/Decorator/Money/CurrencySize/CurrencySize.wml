<div class="controlsDemo__wrapper ws-flexbox ws-justify-content-center">
    <div>
        <ws:for data="size in _baseSizes">
            <div class="controlsDemo__cell">
                <Controls.baseDecorator:Money value="{{_value}}" currency="Dollar" fontSize="{{size}}" currencySize="{{size}}"/>
            </div>
        </ws:for>
    </div>
</div>
