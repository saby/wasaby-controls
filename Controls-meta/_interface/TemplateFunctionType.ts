import { AnyType, Meta } from 'Types/meta';
import { TemplateFunction } from 'UI/Base';

export const TemplateFunctionType = AnyType.id(
    'Controls/meta:TemplateFunctionType'
) as Meta<TemplateFunction>;
