import { FunctionType, Meta } from 'Types/meta';
import { TItemActionVisibilityCallback } from 'Controls/interface';

export const TItemActionVisibilityCallbackType =
    FunctionType as unknown as Meta<TItemActionVisibilityCallback>;
