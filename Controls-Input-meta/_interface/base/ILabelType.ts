import * as rk from 'i18n!Controls-Input';
import { StringType } from 'Meta/types';

/**
 * Тип текстовой метки виджета.
 * @public
 */
export const ILabelType = StringType.id('Controls-Input-meta/interface:ILabelType').title(
    rk('Метка')
);
