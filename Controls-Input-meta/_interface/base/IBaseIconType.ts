import * as rk from 'i18n!Controls-Input';
import { StringType } from 'Meta/types';

/**
 * Тип иконки виджета.
 * @public
 */
export const IBaseIconType = StringType.id('Controls-Input-meta/interface:IBaseIconType').title(
    rk('Иконка')
);
