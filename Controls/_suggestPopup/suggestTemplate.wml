<ws:partial template="wml!Controls/_suggestPopup/WrappedList">
  <Controls.explorer:View scope="{{_options}}"
                          columns="{{_options.columns || _defaultColumns}}"
                          viewMode="search"
                          breadcrumbsVisibility="{{_options.breadcrumbsVisibility || 'hidden'}}"
                          on:itemClick="_itemClick()">
  </Controls.explorer:View>
</ws:partial>
