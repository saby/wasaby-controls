<div class="ws-align-self-center{{ !isEmptyMargin ? ' controls-Tabs_margin-left' }}">
    <div title="{{tooltip}}" class="{{icon}} controls-icon_size-{{iconSize || 's'}}
                         controls-icon_style-{{iconStyle || 'unaccented'}}"></div>
</div>
