<Controls.checkbox:Checkbox caption="{{contentTemplate || item[displayProperty || 'title']}}"
                           triState="{{triState}}"
                           multiline="{{multiline}}"
                           value="{{selected}}"/>
