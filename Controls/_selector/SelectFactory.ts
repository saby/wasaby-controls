import loadData from 'Controls/_selector/SelectFactory/loadData';
import slice from 'Controls/_selector/SelectFactory/Slice';
import getContextConfig from 'Controls/_selector/SelectFactory/getContextConfig';

/**
 * Фабрика окон выбора.
 * @library Controls/selector:Factory
 * @author Герасимов А.М.
 * @public
 */

export default { loadData, slice, getContextConfig };
