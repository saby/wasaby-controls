<ws:partial template="{{_options.content}}"
            scope="{{_options}}"
            compositeViewConfig="{{_compositeViewConfig}}"
            hasItemWithImage="{{_hasItemWithImage}}"
            nodeProperty="{{ _options.useCleanStore ? undefined : _options.nodeProperty }}"
            sourceController="{{ _options.useCleanStore ? undefined : _options.sourceController }}">
</ws:partial>
