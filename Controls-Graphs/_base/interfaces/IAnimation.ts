/**
 * Интерфейс, определяющий будут ли анимироваться "переходы" графика.
 * @public
 */
export default interface IAnimation {
    /**
     * Определяет наличие анимации
     * @default true
     */
    animation?: boolean;
}
