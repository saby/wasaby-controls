import IStoreId from './IStoreId';
import IName from './IName';

/**
 * Интерфейс, описывающий общие опции для connected графиков(связанных с контекстом).
 * @public
 */
export default interface IConnectedGraphProps extends IStoreId, IName {}
