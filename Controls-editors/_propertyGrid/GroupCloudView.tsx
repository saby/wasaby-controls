import { GroupComponent, IGroupComponentProps } from 'Controls-editors/object-type';

export const GroupCloudView = function (props: IGroupComponentProps) {
    return <GroupComponent {...props} className={'controls_PropertyGrid__GroupCloud'} />;
};
